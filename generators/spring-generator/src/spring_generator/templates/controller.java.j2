package {{ project.package_name }}.controller;

import java.util.List;

import {{ project.package_name }}.dto.{{ entity.class_name }}Request;
import {{ project.package_name }}.dto.{{ entity.class_name }}Response;
import {{ project.package_name }}.service.{{ entity.class_name }}Service;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseStatus;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/{{ entity.endpoint_name }}")
@Tag(name = "{{ entity.class_name }}", description = "CRUD de {{ entity.class_name }}")
public class {{ entity.class_name }}Controller {

    private final {{ entity.class_name }}Service service;

    public {{ entity.class_name }}Controller({{ entity.class_name }}Service service) {
        this.service = service;
    }

    @GetMapping
    @Operation(operationId = "list{{ entity.class_name }}", summary = "Listar {{ entity.class_name }}")
    public List<{{ entity.class_name }}Response> findAll() {
        return service.findAll();
    }

    @GetMapping("/{id}")
    @Operation(operationId = "get{{ entity.class_name }}", summary = "Obtener {{ entity.class_name }}")
    public {{ entity.class_name }}Response findById(@PathVariable {{ entity.id_field.java_type }} id) {
        return service.findById(id);
    }

    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    @Operation(operationId = "create{{ entity.class_name }}", summary = "Crear {{ entity.class_name }}")
    public {{ entity.class_name }}Response create(
            @Valid @RequestBody {{ entity.class_name }}Request input) {
        return service.create(input);
    }

    @PutMapping("/{id}")
    @Operation(operationId = "update{{ entity.class_name }}", summary = "Actualizar {{ entity.class_name }}")
    public {{ entity.class_name }}Response update(
            @PathVariable {{ entity.id_field.java_type }} id,
            @Valid @RequestBody {{ entity.class_name }}Request input) {
        return service.update(id, input);
    }

    @DeleteMapping("/{id}")
    @Operation(operationId = "delete{{ entity.class_name }}", summary = "Eliminar {{ entity.class_name }}")
    public ResponseEntity<Void> delete(@PathVariable {{ entity.id_field.java_type }} id) {
        service.delete(id);
        return ResponseEntity.noContent().build();
    }
}
